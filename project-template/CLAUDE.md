# <プロジェクト名>

<!--
  トークン予算: このファイルは全リクエストの入力に乗る固定費。200行/2,000トークン以内を維持(公式推奨は200行以下)。
  ここに書くのは「毎セッション必ず必要な事実」だけ。
  手順書・設計資料・稀にしか使わない知識は .claude/skills/ に置く(必要時のみ読み込まれる)。
  計測: bash ~/.claude/skills/cost-audit/scripts/estimate_tokens.sh CLAUDE.md
-->

## 概要
- 目的: <1〜2行>
- スタック: <言語 / フレームワーク / 主要ライブラリ>

## コマンド
- ビルド: `<cmd>`
- テスト: `<cmd>`(単体は `<cmd> path/to/test` で絞る)
- Lint: `<cmd>`

## 構成(要点のみ)
- `src/...`: <1行>
- `...`

## 規約(このプロジェクト固有のものだけ)
- <汎用的なベストプラクティスは書かない。モデルは知っている>
